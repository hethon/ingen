---
"@hethon/ingen": minor
---

Add a dedicated `generate` subcommand.

Installer generation is now invoked with `ingen generate` instead of the root command. This groups generation-specific options under a dedicated subcommand and provides a more consistent CLI structure for future commands.
