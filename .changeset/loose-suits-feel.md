---
"@hethon/ingen": minor
---

Rename the `target_triple` field to `target`.

The new name is shorter and focuses on what the field represents rather than the naming convention it uses. `ingen` continues to expect target triples as the field's value.
