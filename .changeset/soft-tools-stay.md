---
"@hethon/ingen": minor
---

Improve editor validation for `install_paths`.

The generated JSON Schema now includes the validation rules for `install_paths`, allowing editors with JSON Schema support to detect invalid install paths and report them while you edit the manifest.
