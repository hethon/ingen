---
"@hethon/ingen": minor
---

Rename the manifest field `version` to `app_version`.

The new name makes it explicit that the field refers to the version of the application being installed, and distinguishes it from `ingen`'s own `--version` flag.
